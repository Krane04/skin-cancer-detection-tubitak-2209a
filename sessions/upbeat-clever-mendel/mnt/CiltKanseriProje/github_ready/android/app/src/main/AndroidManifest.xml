<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Kamera erişimi — lezyon görüntülerini çekmek için -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Biyometrik kilit (parmak izi / yüz) -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <!-- Titreşim geri bildirimi -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Bildirim izni (Android 13+) — kontrol hatırlatıcıları -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- NOT: INTERNET iznini eklemiyoruz — tüm analiz cihaz üzerinde,
         tamamen çevrimdışı yapılıyor. Veri asla cihazdan çıkmıyor. -->

    <uses-feature android:name="android.hardware.camera.any" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:name=".MelonomScannerApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MelonomScanner"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MelonomScanner.Splash"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
