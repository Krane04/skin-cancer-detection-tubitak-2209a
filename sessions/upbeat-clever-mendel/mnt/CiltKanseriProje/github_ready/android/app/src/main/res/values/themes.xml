<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base theme for Compose activity -->
    <style name="Theme.MelonomScanner" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
    </style>

    <!-- Splash theme (SplashScreen compat) -->
    <style name="Theme.MelonomScanner.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.MelonomScanner</item>
    </style>
</resources>
